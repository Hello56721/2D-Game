name: 2d-game-by-yue # you probably want to 'snapcraft register <name>'
base: core20 # the base snap is the execution environment for this snap
version: '0.1' # just for humans, typically '1.2+git' or '1.3.2'
summary: A 2D Game by Yue that is very cool # 79 char long summary
description: |
  This is a 2D Game made by Yue. Use the arrow key to move the square 
  around the screen. That's pretty much it.

grade: devel # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots

parts:
  part:
    plugin: dump
    source: ./releases/0.1-linux
    stage_packages:
      - libx11-6
      - libstdc++
      - libgcc-s1
